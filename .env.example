# Copy to .env and adjust. .env is git-ignored and must never contain a real secret.
# Values below are local development placeholders only.

# --- Database ---
TASKFORGE_DATABASE_URL=postgres://taskforge:taskforge@127.0.0.1:5442/taskforge?sslmode=disable

# --- API service ---
TASKFORGE_API_ADDR=127.0.0.1:8080
# Maximum accepted request body, in bytes.
TASKFORGE_MAX_REQUEST_BYTES=262144
# Bounds every handler and the database work it performs. This deadline can
# elapse while acquiring a lock, while executing a statement, or during COMMIT.
# A worker-control request that exhausts it returns HTTP 503
# `service_unavailable`.
#
# A deadline reached during COMMIT leaves the immediate outcome unknown, so the
# response does not promise that nothing committed. The caller should retry the
# identical request using that operation's own identity or fence — the same
# session identity for registration and heartbeat, the same session and claim
# request id for a claim, the same attempt fence for start and succeed, for
# renewal the same fence PLUS the same renewal request id and expected
# generation, and for a failure report or a cancellation acknowledgment the same
# fence PLUS the same outcome request id and the same body. Each is idempotent
# under those identifiers, so a replay returns the already-committed result
# instead of producing a second one.
#
# Generating a FRESH identity after an ambiguous response is the one thing a
# caller must never do: on renewal it extends the lease twice, and on an outcome
# report it consumes a second place in the attempt budget and redraws jitter.
TASKFORGE_API_REQUEST_TIMEOUT=25s
# Server-owned lease window, issued at claim and reissued by every renewal. A
# worker never supplies its own expiry.
#
# Work is NOT limited to one lease. A running attempt renews on
# TASKFORGE_LEASE_RENEW_INTERVAL below, so cooperative work may safely run across
# many lease windows for as long as renewal keeps succeeding. What still bounds
# one execution is the job's own timeout_seconds, which renewal never extends.
#
# How that budget is enforced: the attempt's deadline is stamped ONCE, durably,
# when its start transition commits, and the worker turns the server-measured
# remaining budget into a conservative local deadline. It cancels the handler's
# context at that local deadline, and also as soon as it can no longer prove it
# still holds the lease, or when a cancellation directive arrives.
#
# A cooperative handler observes the cancellation and returns. Go cannot forcibly
# terminate arbitrary handler code, so an uncooperative handler may keep running
# until it returns on its own or the process exits. What is guaranteed is the
# control-plane side, and it is guaranteed in three independent ways: the fenced
# transition is rejected once lease authority is gone, it is rejected once the
# persisted deadline has passed, and it is rejected once cancellation has won.
# The reconciler then records the truthful terminal outcome — TIMED_OUT,
# CANCELED, or ABANDONED — and hands recoverable work to another worker.
#
# Hard termination of an uncooperative handler needs isolated process or
# container execution, which is post-V1.
TASKFORGE_LEASE_DURATION=30s

# --- Liveness and lease renewal ---
# How often a worker proves its process session is alive. The control plane
# stamps PostgreSQL receipt time; a worker-supplied clock is never authoritative.
TASKFORGE_HEARTBEAT_INTERVAL=5s
# How long a current session may go unheard-from before the reconciler marks it
# UNHEALTHY. Must be at least 3x TASKFORGE_HEARTBEAT_INTERVAL, so one lost
# request cannot make a healthy worker look dead. Validated at startup.
TASKFORGE_SESSION_STALE_AFTER=15s
# How often a running attempt renews its lease. Must be at most one third of
# TASKFORGE_LEASE_DURATION, so renewal can fail twice and still succeed before
# the lease PostgreSQL owns expires. Validated at startup.
#
# Renewal is idempotent under a client-generated renewal request id plus the
# expected generation. Retrying an ambiguous renewal MUST reuse both; generating
# a fresh id would extend the lease a second time.
TASKFORGE_LEASE_RENEW_INTERVAL=10s

# --- Reconciler ---
# Repairs durable state no live process will repair: stale sessions, and expired
# leases whose attempts must be abandoned so capacity is released and recoverable
# work is requeued. Safe to run with several replicas.
TASKFORGE_RECONCILER_ADDR=127.0.0.1:8083
TASKFORGE_RECONCILER_POLL_INTERVAL=2s
# Maximum candidates examined per scan, per pass. Bounds one transaction batch,
# not total throughput: the next tick picks up whatever did not fit.
TASKFORGE_RECONCILER_BATCH_SIZE=50

# --- Scheduler ---
# Makes durable work reachable: promotes delayed and retry-waiting jobs once
# PostgreSQL says they are due, and re-notifies queued work whose only advisory
# notification was lost. Safe to run with several replicas: every decision is
# revalidated under row locks, and each promotion names the exact eligibility
# generation it expects.
#
# It holds NO broker connection. It writes the authoritative outbox event and
# taskforge-outbox owns every byte that reaches the broker.
TASKFORGE_SCHEDULER_ADDR=127.0.0.1:8084
TASKFORGE_SCHEDULER_POLL_INTERVAL=2s
# Maximum candidates examined per scan, per pass. Bounds one transaction batch,
# not total throughput: the next tick picks up whatever did not fit.
TASKFORGE_SCHEDULER_BATCH_SIZE=50
# How long a QUEUED job may go without a notification before the scheduler
# treats its notification as lost and writes a replacement.
#
# Two validated relationships, both checked at startup:
#   - at least 3x TASKFORGE_SCHEDULER_POLL_INTERVAL, so an ordinary delivery has
#     had several chances before anything is called lost;
#   - at least TASKFORGE_OUTBOX_CLAIM_TIMEOUT, because a claimed-but-unpublished
#     event is invisible to the pending-event check for exactly that window. A
#     shorter interval would decide an event was lost while a publisher was still
#     in the middle of publishing it.
#
# Re-notification is additionally rate-limited per job and skipped entirely while
# a pending event for the job's CURRENT notification generation still exists, so
# a slow publisher cannot turn into a growing pile of duplicates.
TASKFORGE_SCHEDULER_RENOTIFY_AFTER=60s

# --- Job retry policy ---
# Bounded exponential backoff with proportional jitter, applied to both
# worker-reported retryable failures and reconciler-detected timeouts. One policy
# for both is deliberate: a job must not learn a different cadence depending on
# whether its worker managed to report the failure.
#
#   nominal = min(MAX, BASE * MULTIPLIER^(attempt-1))
#   delay   = clamp(nominal * (1 + JITTER*(2r-1)), 0, MAX)
#   result  = ceil(delay) in whole milliseconds, still bounded by MAX
#
# The delay is STORED in whole milliseconds, so it is quantized to that
# granularity before anything is decided from it. Rounding is upward, so a
# positive backoff never silently becomes an immediate retry; a calculation that
# produces exactly zero stays zero and the job is requeued immediately.
#
# MAX and BASE have different granularity rules, because they are different
# kinds of thing:
#
#   MAX  is a strict upper bound on the stored value, so it must be at least 1ms
#        AND an exact whole-millisecond multiple. Startup fails otherwise. A
#        sub-millisecond maximum would leave no storable delay inside it, and a
#        non-whole one would be silently floored, so the bound that applied
#        would not be the bound that was configured.
#   BASE is an input to the calculation, not a bound on a stored value, so any
#        positive value is allowed. A sub-millisecond base simply rounds up to
#        1ms, provided MAX can hold it.
#
# The result is never larger than MAX and never negative.
#
# The chosen delay AND the instant it produced are persisted on the attempt, so
# an ambiguous failure report replays the committed decision instead of drawing
# fresh jitter for a different retry time.
#
# The random source is injected: seeded in tests, and independently seeded from
# system entropy in each API and reconciler process, so replicas recovering from
# one outage do not compute identical retry instants and stampede.
#
# Crash recovery is deliberately NOT retry. An abandoned attempt consumes the
# attempt budget and requeues immediately with no backoff at all
# (docs/adr/0009-abandoned-attempts-consume-the-attempt-budget.md); these
# settings do not apply to it.
TASKFORGE_JOB_RETRY_BASE=1s
TASKFORGE_JOB_RETRY_MAX=5m
TASKFORGE_JOB_RETRY_MULTIPLIER=2.0
TASKFORGE_JOB_RETRY_JITTER=0.2

# --- Broker (SQS-compatible) ---
TASKFORGE_BROKER_ENDPOINT=http://127.0.0.1:9324
TASKFORGE_BROKER_QUEUE_NAME=taskforge-work-available
TASKFORGE_BROKER_REGION=us-east-1
# ElasticMQ ignores credentials but the AWS SDK requires them to be present.
# These are not secrets and must be replaced by real IAM credentials in AWS.
TASKFORGE_BROKER_ACCESS_KEY_ID=local
TASKFORGE_BROKER_SECRET_ACCESS_KEY=local

# --- Result storage ---
# A result at or above this many bytes is stored in the object store below
# instead of inline in PostgreSQL. Must be positive and strictly less than
# TASKFORGE_MAX_REQUEST_BYTES: an inline result travels inside the fenced
# succeed request body, which is itself subject to that same limit, so a
# threshold at or above it would let a worker classify a result as "small
# enough to inline" that its own reporting request could never actually
# deliver. Validated at startup.
TASKFORGE_RESULT_INLINE_THRESHOLD_BYTES=65536
# S3-compatible object store for a large result. LocalStack's S3 provider
# locally, real S3 in the cloud direction -- see
# docs/adr/0015-result-storage.md for why LocalStack rather than MinIO.
# taskforge-api and taskforge-worker each construct their own client from
# these same settings, exactly as they each construct their own broker
# client from TASKFORGE_BROKER_* above.
TASKFORGE_RESULTS_ENDPOINT=http://127.0.0.1:4566
TASKFORGE_RESULTS_BUCKET=taskforge-results
TASKFORGE_RESULTS_REGION=us-east-1
# LocalStack ignores credentials but the AWS SDK requires them to be
# present, identical to TASKFORGE_BROKER_* above. These are not secrets and
# must be replaced by real IAM credentials in AWS.
TASKFORGE_RESULTS_ACCESS_KEY_ID=local
TASKFORGE_RESULTS_SECRET_ACCESS_KEY=local

# --- Outbox publisher ---
TASKFORGE_OUTBOX_ADDR=127.0.0.1:8081
TASKFORGE_OUTBOX_BATCH_SIZE=50
TASKFORGE_OUTBOX_POLL_INTERVAL=1s
# How long a claimed-but-unpublished event stays invisible to other publishers.
TASKFORGE_OUTBOX_CLAIM_TIMEOUT=30s
TASKFORGE_OUTBOX_BACKOFF_BASE=1s
TASKFORGE_OUTBOX_BACKOFF_MAX=5m
TASKFORGE_OUTBOX_BACKOFF_MULTIPLIER=2.0
TASKFORGE_OUTBOX_BACKOFF_JITTER=0.2

# --- Worker process ---
TASKFORGE_WORKER_API_URL=http://127.0.0.1:8080
# The worker key this process presents when registering a session -- see
# docs/adr/0014-worker-control-authentication.md. Mint one with
# POST /internal/v1/worker-keys (loopback-only operator plumbing, same as
# TASKFORGE_API_ADDR's api-keys route). The key's scope decides which jobs
# this worker can claim: a job submitted under an API key minted for a
# DIFFERENT scope is durable and readable but will never be claimed by a
# worker whose worker key names a different scope -- it stays QUEUED.
#
# Authenticates registration only. Every later control-plane call (heartbeat,
# claim, lease renewal, outcome report) is authenticated by the session
# registration created, not by presenting this credential again -- so
# revoking this key does not retroactively invalidate a call already in
# flight, but does refuse the next call any session it registered makes.
TASKFORGE_WORKER_API_KEY=
TASKFORGE_WORKER_ADDR=127.0.0.1:8082
# Stable logical worker name. `local-worker` is a single-process development
# default. Concurrently running workers MUST each be given a stable, distinct
# name: one logical worker allows only one current process session, so a second
# worker booting under the same name fences the first, and the fenced process
# exits. Keep the name stable across restarts of the same logical worker.
TASKFORGE_WORKER_NAME=local-worker
# Optional override; defaults to the operating-system hostname.
TASKFORGE_WORKER_HOSTNAME=
TASKFORGE_WORKER_GROUP=default
TASKFORGE_WORKER_QUEUE=default
TASKFORGE_WORKER_CONCURRENCY=4
# Comma-separated set declared by this process boot.
TASKFORGE_WORKER_CAPABILITIES=cpu
# Broker long-poll wait. Must be between 1s and 20s: the SQS ReceiveMessage
# request carries whole seconds, so a sub-second value truncates to 0, disables
# long polling, and busy-loops every idle slot.
TASKFORGE_WORKER_POLL_WAIT=10s
TASKFORGE_WORKER_REQUEST_TIMEOUT=10s
TASKFORGE_WORKER_SHUTDOWN_TIMEOUT=15s

# --- Logging ---
TASKFORGE_LOG_LEVEL=info

# --- Integration tests (make test-integration) ---
TASKFORGE_TEST_DATABASE_URL=postgres://taskforge:taskforge@127.0.0.1:5442/taskforge?sslmode=disable
